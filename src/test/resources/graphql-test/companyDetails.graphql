query companyById($id: ID) {
    companyById(id: $id) {
        id
        SIC
        name
        status
        category
        companyNumber
        countryOfOrigin
        incorporationDate
        mortgagesOutstanding
    }
 }